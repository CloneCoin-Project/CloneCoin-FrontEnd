import styled from 'styled-components';
import { Layout } from 'antd';

import { BLACK_WHITE } from '@assets/color';

export const LayoutPageContainer = styled(Layout)`
  height: auto !important;
  background-color: ${BLACK_WHITE[3]};
`;
